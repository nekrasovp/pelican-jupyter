# Provenance and license

## Repository origin

`nekrasovp/pelican-jupyter` was created on 2026-07-21 with GitHub's standard
fork mechanism from the public, archived
[`danielfrg/pelican-jupyter`](https://github.com/danielfrg/pelican-jupyter)
repository. It is a successor-maintenance fork, not an independently recreated
repository.

At fork creation:

- the upstream and fork default branches were both `main`;
- both `main` refs resolved to
  `3c82c279d74b47b691b67e224cc477e32456326c`;
- both histories resolved to the same root commit,
  `bc66e380afa6b2a17dfed129f19fe0987617c695`;
- GitHub reported `danielfrg/pelican-jupyter` as the fork parent and source.

The upstream authors and contributors remain visible in the unchanged Git
history. Successor changes are added after the shared base; upstream commits are
not rewritten, squashed, or reattributed.

## Independence and names

The archived upstream author and the Pelican project have not endorsed this
fork. No repository, organization, or package ownership beyond the visible
GitHub fork relationship should be inferred.

The repository identity `nekrasovp/pelican-jupyter` is the temporary working
successor fork for development and end-to-end plugin/site/theme validation.
[ADR 0001](decisions/0001-fork-first-development-identity.md) defers outreach
and the final distribution identity until that validation is complete and a
new explicit user decision resumes the work. The repository/package names,
import paths, and compatibility behavior are unchanged by that decision.

No package ownership, publication permission, upstream endorsement, or
organization acceptance is inferred. No package publication, transfer, or
outreach has occurred in this successor project.

## License and attribution

The repository remains licensed under the Apache License, Version 2.0. The
complete upstream `LICENSE.txt` is preserved unchanged at the repository root.
The package manifest declares the Apache license and records both the successor
source repository and archived upstream repository.

Copyright and attribution remain with their respective authors and
contributors. The fork relationship, this note, and the preserved Git history
identify the origin of the work and the successor modifications.
